validatorの中で、日本語のLv:を目安にして、ユーザーごとの


cloud vision　Apiから、返ってくる位置情報は、四角の四隅を指定されたもの。
一つの文字につき、４点の位置情報が返ってくるのはすごい。

cloud visionの公式サンプルが、リスト内包表記を使用している
難しいな。

data = [1, 2, 3, 4, 5] この配列について、各要素の値を2倍にする処理を通常のfor文で記述すると、以下のようになります。
# data配列の中身を2倍にする
newData = []
for d in data:
  newData.append(d * 2) この3行の実装を、リスト内包表記で記述すると1行で記述することができます。素敵！
# data配列の中身を2倍にする
newData = [d * 2 for d in data] この書き方はシンプルだし直感的に理解できるので、なかなか便利です。